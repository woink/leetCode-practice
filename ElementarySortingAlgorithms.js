// Sorting i the process of rearranging the items in a collection (e.g. an array) so that the items are in some kind of order
    // Sorting numbers from smalles to largest
    // Sorting names by alpha
    // Sorting movies base on release year
    // Sorting movies based on revenue

function sort(arr) {

  return arr
}

sort([23, 45, 6, 12, 13])